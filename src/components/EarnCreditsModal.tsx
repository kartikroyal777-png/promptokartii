import React from 'react';

// This component is no longer used and has been deprecated.
// The app now redirects to the /earn page instead of showing a modal.
const EarnCreditsModal: React.FC = () => {
  return null;
};

export default EarnCreditsModal;
